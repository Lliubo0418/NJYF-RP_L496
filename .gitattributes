# ============================================================
#  .gitattributes — 换行符 & 二进制规范
#  （Windows Keil + 跨平台协作）
# ============================================================

# ------------------------------------------------------------
#  默认：文本文件统一 LF，自动转换
# ------------------------------------------------------------
* text=auto eol=lf

# ------------------------------------------------------------
#  Keil 工程文件：强制 CRLF
#  （µVision 在 Windows 下对换行符敏感，避免乱码 / 一堆 diff）
# ------------------------------------------------------------
*.uvprojx text eol=crlf
*.uvproj  text eol=crlf
*.uvoptx  text eol=crlf
*.ioc     text eol=crlf

# ------------------------------------------------------------
#  源码 / 脚本：明确 LF
# ------------------------------------------------------------
*.c   text eol=lf
*.h   text eol=lf
*.cpp text eol=lf
*.hpp text eol=lf
*.s   text eol=lf
*.S   text eol=lf
*.ld  text eol=lf
*.sct text eol=lf

# ------------------------------------------------------------
#  固件镜像：标记二进制，禁止 diff / 合并
# ------------------------------------------------------------
*.hex binary
*.bin binary
*.axf binary
*.elf binary

# ------------------------------------------------------------
#  行尾规范化（提交时统一）
# ------------------------------------------------------------
* -text=auto
